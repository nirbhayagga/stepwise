{
  "name": "stepwise",
  "private": true,
  "version": "1.1.1",
  "homepage": "https://stepwise.nirbhay.dev",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "lighthouse": "lhci autorun",
    "docker:build": "docker build -t stepwise .",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.4.29",
    "vue-router": "^4.0.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-vue": "^5.0.5",
    "typescript": "^5.2.2",
    "vite": "^5.3.1",
    "vue-tsc": "^2.0.21",
    "wrangler": "^4.127.1"
  }
}
